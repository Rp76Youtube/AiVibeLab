# Lab rules

1. Run only on a computer you own and control.
2. Keep the service bound to `127.0.0.1`.
3. Use only the fictional data included in the seeders.
4. Never reuse the payloads against a public or third-party service.
5. Reset with `php artisan migrate:fresh --seed` after testing.
6. Do not deploy this repository, even temporarily.
